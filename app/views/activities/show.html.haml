%ul
	%li
		%h4= @activity.title
	%li= @activity.description
	%li= @activity.address

	= link_to 'Join activity', memberships_path(:user_id => @user, :activity_id => @activity), :method => :post


%ul
	%li
		%h4 Organiser
		= @owner

%ul
	%li
		%h4 Members
		- @activity.users.each do |user|
			%li= user.name
